__pycache__/
*.pyc
data/outputs/
data/debug_model_inputs/
.venv_moondream/
.venv_deepseek_vl2/
.venv_hunyuan_ocr/
config/workspace.local.yaml
scripts/_phase2_checksum_baseline.json

# Generated sensor data - workspace-owned per docs/RUN_ARCHITECTURE.md,
# and >100MB (GitHub hard-rejects it; removed from history 2026-08-14)
data/baseline_embeddings.json

# Windows desktop shortcuts (machine-specific, not portable)
*.lnk

# Archive/reference baseline data - too large for git (GB-scale),
# not portable to other clones. See feedback_archive_exclusion_rule
# memory note - these are archival, not active project inputs.
data/reference_prerefactor/
data/archive_census_only_run/
